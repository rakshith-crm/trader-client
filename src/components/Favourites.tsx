import { Grid, Typography, Divider } from "@mui/material";
import React from "react";

const Favourites = () => {
  return (
    <>
      <Typography sx={{ marginTop: 2 }}>Favourites</Typography>
    </>
  );
};

export default Favourites;
